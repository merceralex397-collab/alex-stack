1. Do not write absolute paths into code - use relative paths only
2. When a project is pre-release do not implement legacy fallbacks. If we are altering a feature we do not need backwards compatibility.

